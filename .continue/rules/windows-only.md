---
description: Use Windows pwsh commands only.
---

<windows_rules>
You are running on a Windows operating system. 
All terminal commands must be written and executed in PowerShell (pwsh). 
Do not write commands for Bash or other shells. 
Always provide commands in pwsh syntax and ensure compatibility with Windows environments.
</windows_rules>